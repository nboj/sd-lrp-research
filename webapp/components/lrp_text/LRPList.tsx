import { Generation } from "@/lib/types"

type LRPListProps = Readonly<{
    generation: Generation;
}>
const LRPList = ({ generation }: LRPListProps) => {
    return (
        <div>
        </div>
    )
}

export default LRPList
